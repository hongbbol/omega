# 제품 데이터 자동 수집 루트 (캡처 불필요)

> 2026-06 갱신. 이전 `수집_요청목록.md`는 "상세페이지 캡처가 필요하다"는 전제였으나,
> **현재 실행 환경에서는 주요 한국 펫 제조사/유통 사이트에 직접 접근이 가능**해
> 캡처 없이 자동 수집이 성립한다. 이 문서가 수집 표준 루트다.

## 왜 캡처가 필요 없어졌나 (환경 재점검 결과)

| 사이트 | 접근 | 비고 |
|---|---|---|
| choandkang.com (조공) | ✅ 200 | Cafe24 — 상세 이미지 URL을 HTML에서 바로 추출 |
| fitpet.co.kr (핏펫) | ✅ 200 | SPA일 수 있음 → 필요 시 `--render` |
| danawa.com (다나와) | ✅ 200 | 사람용 제품 스펙 텍스트 많음 |
| iherb.com / naver | ❌ 403 | Cloudflare/봇차단 — 2차 출처로 우회 |

핵심 스펙(1캡슐당 EPA/DHA mg, IFOS/COA 표)은 여전히 **상세페이지 이미지에 잠겨** 있다.
그러나 그 이미지를 **자동으로 내려받아 OCR + Claude 비전으로 판독**하면 사람이 캡처할 필요가 없다.

## 파이프라인 (`collect.py`)

```
requests(브라우저 UA) → 상세 HTML
   └ (JS 렌더 필요 시) playwright/chromium  --render
상세 본문 이미지 URL 자동 추출 → harvest/<번호>/ 다운로드
   └ tesseract(kor+eng) OCR → ocr.txt
정규식 추출 → products_draft.csv (초안 + 근거 + 신뢰도 + 플래그)
   └ 모호/충돌 항목은 비우고 '비전확인필요' 플래그 + SF 이미지 경로 기록
Claude 비전(Read)으로 SF 패널 정밀 재확인 → 최종 확정 → products.csv 병합
```

### 2단 정확도 설계 (중요)
상세페이지에는 **제품 비교표·마케팅 카피**가 섞여 있어 정규식 단독은 오추출한다
(예: 비교표의 "EPA 110mg"를 밀도로 오인). 그래서:

1. **밀도**는 정식 *Supplement Facts* 형태 `EPA n mg + DHA n mg`만 신뢰(비교표와 구분, 소수 허용).
2. **순도**는 SF 패널의 `EPA+DHA ÷ 총오일`로 **계산**(라벨 표기 순도보다 신뢰도 높음).
   예) 110mg / 137.6mg = 80%, 110mg / 146.67mg = 75%.
3. 값이 충돌하거나 못 읽으면 **비우고 플래그** — 추정 입력 금지.
4. 플래그 항목·핵심 수치는 `harvest/<번호>/<SF이미지>.jpg`를 **Claude 비전으로 재확인**.

## 사용법

```bash
pip install requests beautifulsoup4 lxml playwright
apt-get install -y tesseract-ocr tesseract-ocr-kor          # OCR
python -m playwright install --with-deps chromium           # --render 쓸 때만

# sources.csv 에 '번호,url,render' 행을 추가한 뒤:
python collect.py            # 전체
python collect.py 3 4        # 특정 번호
python collect.py --render 16  # JS 렌더 필요한 사이트
```

`sources.csv` 예시:
```
번호,url,render
3,https://choandkang.com/product/.../705/...,0
16,https://kaikoora.com/...,1
```

### URL 찾기 (Cafe24류 자동 탐색)
```bash
curl -s -A "Mozilla/5.0 ... Chrome/124" \
  "https://choandkang.com/product/search.html?keyword=오메가" \
  | grep -oE "/product/[^\"']*/[0-9]+/category/[0-9]+/display/1/" | sort -u
```

## 검증 사례 (캡처 없이 수동 데이터 재현)

| 번호 | 제품 | 자동수집 결과 | 기존 수동 캡처값 |
|---|---|---|---|
| 3 | 조공 레드 오메가3 미니 | 밀도110·순도80·명시·rTG·소형어·항산화있음·캡슐·인증·COA공개·원료사명시 | 동일 |
| 4 | 조공 데일리 퓨어 미니 | 밀도110·순도75·명시·rTG·소형어·항산화있음·캡슐·인증·COA공개·원료사명시 | 동일 |

(포장은 비교표에 블리스터/통이 혼재해 자동 보류 → 비전 재확인 항목으로 처리.)

## 산출물 위치
- `harvest/<번호>/NN.jpg` — 내려받은 상세 이미지(= 캡처 대체물). git 미추적.
- `harvest/<번호>/ocr.txt`, `proposal.json` — OCR 원문·추출 제안. git 미추적.
- `products_draft.csv` — 검수 전 초안. **검수 후 손으로 `products.csv`에 병합.** git 미추적.

## 다음 단계
- `sources.csv`에 #2·#7·#13~#21·#23 URL을 채워 1회 실행(핏펫/카이쿠라는 `--render` 권장).
- 병원/클리닉 채널(#9·#24·#25 등)은 공개정보가 적어 자동 수집도 한계 — 2차 출처 의존.
